# CLAUDE.md

Read `AGENTS.md` first — it is the authoritative agent guidance for this repo: development conventions, the
GitHub-native SWE pipeline, and the issue-disposition contract all live there. This file is only a loader:
the Claude substrate auto-loads `CLAUDE.md`, not `AGENTS.md`, so this pointer exists to close that gap. It
is deliberately not a copy — `AGENTS.md` stays the single source of truth; keep this file to the pointer
above.
