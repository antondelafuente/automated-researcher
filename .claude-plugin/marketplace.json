{
  "name": "aar-skills",
  "owner": { "name": "Anton de la Fuente" },
  "metadata": {
    "description": "Modular research-agent skills: remote GPU jobs, adversarial claim verification. Extracted from a working autonomous-research lab."
  },
  "plugins": [
    {
      "name": "gpu-job",
      "source": "./plugins/gpu-job",
      "description": "Give your coding agent GPU powers: disposable cloud GPU pods with your environment restored, detached job execution, artifact persistence, verified teardown (never bill an idle GPU). Backend: RunPod."
    },
    {
      "name": "verify-claims",
      "source": "./plugins/verify-claims",
      "description": "Verification gates that catch the confidently-wrong-number class before money or conclusions move: deterministic full-pool data audits plus adversarial fact/design/data/evidence checks read by an independent model family (read-only sandboxed)."
    },
    {
      "name": "experiment-lifecycle",
      "source": "./plugins/experiment-lifecycle",
      "description": "Run GPU experiments like a researcher — the full lifecycle: design-experiment (pre-register a DESIGN.md, clear it with the human through cross-family validity gates, emit a START.md brief + CHECKLIST.md, dispatch to a fresh-context executor) and run-experiment (the zero-context executor: self-wake, acquire/provision/drive/collect/close). Instance specifics live in an execution profile; pairs with gpu-job and verify-claims."
    }
  ]
}
