# DATA AUDIT MANIFEST — <exp> / <dataset>

The "what this data SHOULD be" that the cross-family data auditor (`audit_experiment.sh --data`) reads so it
can say "this VIOLATES the experiment," not just "looks okay." Designer states intent + invariants; the
executor fills paths/hashes/counts after generating. One manifest per distinct dataset.

<!-- DESIGNER: cite DESIGN.md, don't restate it (automated-researcher#817). What is DATASET-OPERATIONAL is
     this file's own — paths, hashes, counts, sources, the transformations applied, the dataset-specific
     invariants only this surface has. Everything the design already pins (purpose, arms, metric,
     comparability, what would invalidate the experiment) is REFERENCED by heading — "purpose per
     `DESIGN.md` § What's measured" — so a design-audit ACCEPT amends ONE file and this manifest cannot
     drift from the design it is auditing against. -->

## Purpose
`DESIGN.md § <heading>` — cite it; add only what is specific to THIS dataset and not stated there.
(e.g. "purpose per `DESIGN.md § Purpose`; this surface is the on-policy long-CoT replay half — base-model
responses to the experiment's prompt set, mixed with trait data.")

## Files / paths / hashes
- <local path or storage URI>  sha256=<...>  rows=<n>
- deterministic audit output: <surface>_data_audit.json  (from the `verify-claims` skill's `audit_data.py`)
- stratified sample: <surface>_data_audit_sample.jsonl

## Sources & intended counts
- <source>: <intended n>  ...   (e.g. no_robots 2779, numina_cot 1063, ...)  — flag if actual ≠ intended

## Transformations applied
<what was done to get here: generated by which model with what config; filtered how; flattened multi-turn how;
split how; relabeled how. Each transformation is a place a bug can enter.>

## Known invariants (what the auditor + audit_data.py should hold the data to)
- Design-level invariants: `DESIGN.md § <heading>` (cited, not restated). Below: only the ones specific to
  THIS surface.
- <e.g. every row finish_reason==stop, closed </think>, non-empty answer (CoT replay)>
- <e.g. prompt distribution = the intended source mix, not an off-distribution set>
- <e.g. responses are generated by the intended model (on-model), with REAL reasoning, not degenerate>
- <e.g. Phase-A and Phase-B sets are DISJOINT (row-id manifests)>
- <e.g. labels/arms: every row tagged with the correct arm; balance as designed>

## What would INVALIDATE the experiment (the auditor's north star)
Experiment-level: `DESIGN.md § <heading>`. Below: the ways bad data on THIS surface specifically would
silently produce a confidently-wrong result —
<e.g. "truncated CoT
trains the model to stop reasoning (the empty-think collapse); off-distribution prompts confound the
distribution test; a descriptor confound makes the trait unattributable">
